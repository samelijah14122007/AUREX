import { motion } from "framer-motion"; import { Eye, EyeOff } from "lucide-react"; import { useState, type InputHTMLAttributes } from "react";
type AuthInputProps=InputHTMLAttributes<HTMLInputElement>&{label:string};
export default function AuthInput({label,type,...props}:AuthInputProps){const [visible,setVisible]=useState(false);const password=type==="password";return <motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="mb-5"><label className="mb-2 block text-sm font-medium text-slate-200">{label}</label><div className="relative"><input {...props} type={password?(visible?"text":"password"):type} className="h-12 w-full rounded-xl border border-white/10 bg-white/5 px-4 pr-11 text-white placeholder:text-slate-500 outline-none transition focus:border-cyan-400 focus:bg-white/10 focus:ring-2 focus:ring-cyan-400/20"/>{password&&<button type="button" aria-label={visible?"Hide password":"Show password"} onClick={()=>setVisible(!visible)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-500 hover:text-cyan-300">{visible?<EyeOff size={18}/>:<Eye size={18}/>}</button>}</div></motion.div>}
